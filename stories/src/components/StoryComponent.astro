---
const components = await Astro.glob(
  '../../node_modules/fulldev-ui/src/components/**/*.astro'
)

const { component, ...rest } = Astro.props

const Component = components.find((c) => c.file.endsWith(component + '.astro'))
---

{Component ? <Component {...rest} /> : ''}
